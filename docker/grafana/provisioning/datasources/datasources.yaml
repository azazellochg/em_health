apiVersion: 1

datasources:
  - name: tem
    type: postgres
    uid: hm-tem
    access: proxy
    isDefault: true
    url: "emhealth-db:5432"
    user: "grafana"
    secureJsonData:
      password: "$POSTGRES_GRAFANA_PASSWORD"
    jsonData:
      database: tem
      sslmode: disable
      postgresVersion: 1500
      timescaledb: true

  - name: sem
    type: postgres
    uid: hm-sem
    access: proxy
    isDefault: false
    url: "emhealth-db:5432"
    user: "grafana"
    secureJsonData:
      password: "$POSTGRES_GRAFANA_PASSWORD"
    jsonData:
      database: sem
      sslmode: disable
      postgresVersion: 1500
      timescaledb: true
