{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {
          "type": "grafana",
          "uid": "-- Grafana --"
        },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "target": {
          "limit": 100,
          "matchAny": false,
          "tags": [],
          "type": "dashboard"
        },
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": 146,
  "links": [
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": false,
      "keepTime": true,
      "tags": [
        "overview",
        "sem"
      ],
      "targetBlank": false,
      "title": "Overviews",
      "tooltip": "",
      "type": "dashboards",
      "url": ""
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": true,
      "keepTime": true,
      "tags": [
        "module",
        "sem"
      ],
      "targetBlank": false,
      "title": "Modules",
      "tooltip": "",
      "type": "dashboards",
      "url": ""
    },
    {
      "asDropdown": false,
      "icon": "external link",
      "includeVars": false,
      "keepTime": true,
      "tags": [
        "dev",
        "sem"
      ],
      "targetBlank": true,
      "title": "",
      "tooltip": "",
      "type": "dashboards",
      "url": ""
    }
  ],
  "panels": [
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${sourceSEM}"
      },
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "auto"
            },
            "inspect": false
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": 0
              }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": {
        "h": 5,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "id": 36,
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true
      },
      "pluginVersion": "12.1.0",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${sourceSEM}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH strips AS (\n  SELECT\n    instrument_id,\n    param_id,\n    substring(subsystem FROM '^Ava(.+)$') AS type,\n    component AS model\n  FROM parameters\n  WHERE instrument_id = $instrument\n    AND subsystem IN ('AvaBaa', 'AvaBda', 'AvaFib', 'AvaSem')\n    AND component ILIKE '%strip%'\n),\n\nreplacement AS (\n  SELECT DISTINCT ON (d.instrument_id, d.param_id)\n         d.instrument_id,\n         d.param_id,\n         d.time AS last_changed\n  FROM data d\n  JOIN strips s USING (instrument_id, param_id)\n  WHERE d.value_num = 0\n  ORDER BY d.instrument_id, d.param_id, d.time DESC\n)\n\nSELECT\n    UPPER(s.type) AS \"Type\",\n    s.model AS \"Model\",\n    COUNT(s.param_id) AS \"Total apertures\",\n    CASE\n        WHEN COUNT(r.param_id) >= 2\n        THEN MAX(r.last_changed)\n        ELSE NULL\n    END AS \"Last replacement\"\nFROM strips s\nLEFT JOIN replacement r\n  USING (instrument_id, param_id)\nGROUP BY s.type, s.model\nORDER BY s.type\n",
          "refId": "A",
          "sql": {
            "columns": [
              {
                "parameters": [],
                "type": "function"
              }
            ],
            "groupBy": [
              {
                "property": {
                  "type": "string"
                },
                "type": "groupBy"
              }
            ],
            "limit": 50
          }
        }
      ],
      "title": "Aperture strips",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": {
        "h": 1,
        "w": 24,
        "x": 0,
        "y": 5
      },
      "id": 4,
      "panels": [],
      "title": "Details",
      "type": "row"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${sourceSEM}"
      },
      "description": "",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "auto"
            },
            "inspect": false
          },
          "mappings": [],
          "noValue": "N/A",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": 0
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Avg daily usage"
            },
            "properties": [
              {
                "id": "unit",
                "value": "h"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Target current"
            },
            "properties": [
              {
                "id": "unit",
                "value": "amp"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg beam current"
            },
            "properties": [
              {
                "id": "unit",
                "value": "amp"
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg degradation"
            },
            "properties": [
              {
                "id": "unit",
                "value": "percent"
              },
              {
                "id": "thresholds",
                "value": {
                  "mode": "absolute",
                  "steps": [
                    {
                      "color": "green",
                      "value": 0
                    },
                    {
                      "color": "yellow",
                      "value": 20
                    }
                  ]
                }
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "color-background"
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 19,
        "w": 12,
        "x": 0,
        "y": 6
      },
      "id": 38,
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true,
        "sortBy": [
          {
            "desc": false,
            "displayName": "Target current"
          }
        ]
      },
      "pluginVersion": "12.1.0",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${sourceSEM}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH\n-- Get the param_ids for the parameters we need\nparam_ids AS (\n    SELECT\n        instrument_id,\n        MAX(param_id) FILTER (WHERE param_name IN ('BeamCurrentFIBParameter', 'BeamCurrentMeasuredParameter')) AS measured_param,\n        MAX(param_id) FILTER (WHERE param_name = 'BeamCurrentTargetParameter') AS target_param,\n        MAX(param_id) FILTER (WHERE param_name = 'SelectedBDAIndexParameter') AS index_param\n    FROM parameters\n    WHERE instrument_id = $instrument\n      AND component = 'FIB'\n    GROUP BY instrument_id\n),\n\nerosion_params AS (\n    SELECT\n        instrument_id,\n        param_id,\n        (substring(param_name FROM '(\\d+)$')::int + 1) AS aperture_idx\n    FROM parameters\n    WHERE instrument_id = $instrument\n      AND subsystem IN ('AvaBda', 'AvaFib')\n      AND component ILIKE '%strip%'\n),\n\n-- find zero erosion values for each aperture\nreplacement AS (\n    SELECT\n        d.param_id,\n        MAX(d.time) AS last_changed,\n        p.aperture_idx\n    FROM data d\n    JOIN erosion_params p USING (instrument_id, param_id)\n    WHERE d.value_num = 0\n    GROUP BY d.param_id, p.aperture_idx\n),\n\ntime_range AS (\n    SELECT\n        COALESCE(MAX(last_changed), to_timestamp($__from/1000)) AS time_from,\n        to_timestamp($__to/1000) AS time_to\n    FROM replacement\n),\n\n-- Extract target current change intervals. The BDA index is changed a few seconds later\ntarget_changes AS (\n    SELECT\n        start_time,\n        end_time,\n        state/1e12 AS target_current\n    FROM state_int_timeline(\n            (\n                SELECT state_agg(d.time, (d.value_num*1e12)::bigint) -- use pA to convert to integers\n                FROM data d\n                         JOIN param_ids p\n                              ON p.instrument_id = d.instrument_id\n                                  AND p.target_param = d.param_id\n                WHERE d.time >= (SELECT time_from FROM time_range)\n                  AND d.time < (SELECT time_to FROM time_range)\n        )\n    )\n),\n\n-- Lateral join data table, for each interval we find the index and median measured current\nresults AS (\n    SELECT tc.start_time,\n           tc.end_time,\n           agg.bda_index,\n           tc.target_current,\n           agg.measured_current\n    FROM target_changes tc\n             CROSS JOIN param_ids p\n             JOIN LATERAL (\n        SELECT MAX(CASE WHEN d.param_id = p.index_param THEN d.value_num::int END) AS bda_index,\n               approx_percentile(0.5, percentile_agg(CASE WHEN d.param_id = p.measured_param THEN d.value_num END)) AS measured_current\n        FROM data d\n        WHERE d.instrument_id = p.instrument_id\n          AND d.param_id IN (p.index_param, p.measured_param)\n          AND d.time >= tc.start_time\n          AND d.time < tc.end_time\n        ) agg ON true\n    ORDER BY tc.start_time\n),\n\n-- Fill the NULL indices with LOCF, filter out abnormal currents and compute degradation %\nresults_filtered AS (\n    SELECT r.start_time AS time,\n           COALESCE(r.bda_index, (SELECT r2.bda_index\n                                  FROM results r2\n                                  WHERE r2.end_time < r.end_time\n                                    AND r2.bda_index IS NOT NULL\n                                  ORDER BY r2.start_time DESC\n                                  LIMIT 1)) AS bda_index,\n           r.target_current,\n           r.measured_current,\n           (r.measured_current - r.target_current) / r.target_current * 100.0 AS degradation\n    FROM results r\n    WHERE r.measured_current > 0.5 * r.target_current\n      AND r.measured_current < 2.5 * r.target_current\n    ORDER BY r.start_time\n),\n\n-- Aggregate per day per BDA index\ndaily_current_stats AS (\n    SELECT time_bucket('1 day', time)                               AS day,\n           bda_index,\n           target_current,\n           approx_percentile(0.5, percentile_agg(measured_current)) AS avg_current,\n           approx_percentile(0.5, percentile_agg(degradation))      AS avg_degradation\n    FROM results_filtered\n    GROUP BY day, bda_index, target_current\n),\n\nranked_days AS (\n    SELECT\n        *,\n        DENSE_RANK() OVER (\n            PARTITION BY bda_index, target_current\n            ORDER BY day DESC\n            ) AS day_rank\n    FROM daily_current_stats\n),\n\n-- Aggregate current/degradation for the last 3 days\ndegradation AS (\n    SELECT bda_index,\n           target_current,\n           approx_percentile(0.5, percentile_agg(avg_current))     AS avg_current_3d,\n           approx_percentile(0.5, percentile_agg(avg_degradation)) AS avg_degradation_3d\n    FROM ranked_days\n    WHERE day_rank <= 3\n    GROUP BY bda_index, target_current\n),\n\n-- Calculate stats for erosion time\nerosion_time AS (\n    SELECT DISTINCT ON (p.aperture_idx)\n        p.aperture_idx,\n        d.value_num AS erosion_time\n    FROM data d\n             JOIN erosion_params p USING (param_id)\n    WHERE d.instrument_id = $instrument\n    ORDER BY p.aperture_idx, d.time DESC\n),\n\ndaily_erosion AS (\n    SELECT\n        time_bucket_gapfill(\n                '1 day',\n                day,\n                start  => (SELECT time_from FROM time_range),\n                finish => (SELECT time_to FROM time_range)\n        ) AS day,\n        aperture_idx,\n        COALESCE(daily_erosion, 0) AS erosion\n    FROM fib_bda_counters_daily\n    WHERE instrument_id = $instrument\n      AND day >= (SELECT time_from FROM time_range)\n      AND day < (SELECT time_to FROM time_range)\n),\n\navg_erosion AS (\n    SELECT\n        aperture_idx,\n        approx_percentile(0.5, percentile_agg(erosion)) AS avg_daily_erosion\n    FROM daily_erosion\n    GROUP BY aperture_idx\n)\n\nSELECT\n    p.aperture_idx AS \"Aperture index\",\n    d.target_current AS \"Target current\",\n    d.avg_current_3d AS \"Avg beam current\",\n    d.avg_degradation_3d AS \"Avg degradation\",\n    e.erosion_time/3600.0 AS \"Erosion time (h)\",\n    du.avg_daily_erosion/3600.0 AS \"Avg daily usage\"\nFROM degradation d\nJOIN erosion_params p ON p.aperture_idx = d.bda_index\nJOIN erosion_time e USING (aperture_idx)\nLEFT JOIN avg_erosion du USING (aperture_idx)\nORDER BY p.aperture_idx, d.target_current\n",
          "refId": "A",
          "sql": {
            "columns": [
              {
                "parameters": [],
                "type": "function"
              }
            ],
            "groupBy": [
              {
                "property": {
                  "type": "string"
                },
                "type": "groupBy"
              }
            ],
            "limit": 50
          }
        }
      ],
      "title": "FIB beam decelerating apertures (BDA)",
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${sourceSEM}"
      },
      "description": "",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "auto"
            },
            "inspect": false
          },
          "mappings": [],
          "noValue": "N/A",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": 0
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Total usage"
            },
            "properties": [
              {
                "id": "unit",
                "value": "percentunit"
              },
              {
                "id": "decimals",
                "value": 0
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "color-background"
                }
              },
              {
                "id": "thresholds",
                "value": {
                  "mode": "absolute",
                  "steps": [
                    {
                      "color": "green",
                      "value": 0
                    },
                    {
                      "color": "yellow",
                      "value": 1
                    },
                    {
                      "color": "orange",
                      "value": 1.15
                    },
                    {
                      "color": "green",
                      "value": 999
                    }
                  ]
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg daily usage"
            },
            "properties": [
              {
                "id": "unit",
                "value": "h"
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 19,
        "w": 12,
        "x": 12,
        "y": 6
      },
      "id": 37,
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true
      },
      "pluginVersion": "12.1.0",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${sourceSEM}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH erosion_params AS (\n    SELECT\n        instrument_id,\n        param_id,\n        component,\n        (substring(param_name FROM '(\\d+)$')::int + 1) AS aperture_idx\n    FROM parameters\n    WHERE instrument_id = $instrument\n      AND subsystem = 'AvaBaa'\n      AND component ILIKE '%strip%'\n),\n\ncomponent_value AS (\n    SELECT component\n    FROM erosion_params\n    LIMIT 1\n),\n\nlifetimes AS (\n    SELECT\n        CASE component\n            WHEN 'BaaStripZeus_PN1112942'\n                THEN ARRAY[300,300,300,1000,1000,1500,1500,500,NULL]\n            ELSE NULL\n        END AS lifetime\n    FROM component_value\n),\n\nerosion_time AS (\n    SELECT DISTINCT ON (p.aperture_idx)\n        p.aperture_idx,\n        d.value_num/3600.0 AS erosion_time\n    FROM data d\n    JOIN erosion_params p USING (instrument_id, param_id)\n    ORDER BY p.aperture_idx, d.time DESC\n),\n\ndaily_usage AS (\n    SELECT\n        time_bucket_gapfill(\n            '1 day',\n            day,\n            start  => $__timeFrom(),\n            finish => $__timeTo()\n        ) AS day,\n        aperture_idx,\n        daily_erosion\n    FROM fib_baa_counters_daily\n    WHERE $__timeFilter(day)\n),\n\navg_daily_usage AS (\n    SELECT\n        aperture_idx,\n        avg(COALESCE(daily_erosion, 0)) AS avg_daily_usage\n    FROM daily_usage\n    GROUP BY aperture_idx\n)\n\nSELECT\n    p.aperture_idx AS \"Aperture index\",\n    e.erosion_time AS \"Erosion time (h)\",\n    l.lifetime[p.aperture_idx] AS \"Lifetime (h)\",\n    du.avg_daily_usage/3600.0 AS \"Avg daily usage\"\nFROM erosion_params p\nJOIN erosion_time e USING (aperture_idx)\nLEFT JOIN avg_daily_usage du USING (aperture_idx)\nCROSS JOIN lifetimes l\nORDER BY p.aperture_idx\n",
          "refId": "A",
          "sql": {
            "columns": [
              {
                "parameters": [],
                "type": "function"
              }
            ],
            "groupBy": [
              {
                "property": {
                  "type": "string"
                },
                "type": "groupBy"
              }
            ],
            "limit": 50
          }
        }
      ],
      "title": "FIB beam accelerating apertures (BAA)",
      "transformations": [
        {
          "id": "calculateField",
          "options": {
            "alias": "Total usage",
            "binary": {
              "left": {
                "matcher": {
                  "id": "byName",
                  "options": "Erosion time (h)"
                }
              },
              "operator": "/",
              "right": {
                "matcher": {
                  "id": "byName",
                  "options": "Lifetime (h)"
                }
              }
            },
            "mode": "binary",
            "reduce": {
              "reducer": "sum"
            },
            "replaceFields": false,
            "unary": {
              "fieldName": "Lifetime (h)",
              "operator": "percent"
            }
          }
        }
      ],
      "type": "table"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "${sourceSEM}"
      },
      "description": "",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "auto"
            },
            "inspect": false
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": 0
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Erosion time (h)"
            },
            "properties": [
              {
                "id": "thresholds",
                "value": {
                  "mode": "absolute",
                  "steps": [
                    {
                      "color": "green",
                      "value": 0
                    },
                    {
                      "color": "yellow",
                      "value": 5000
                    }
                  ]
                }
              },
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "color-background"
                }
              }
            ]
          }
        ]
      },
      "gridPos": {
        "h": 17,
        "w": 4,
        "x": 0,
        "y": 25
      },
      "id": 29,
      "options": {
        "cellHeight": "sm",
        "footer": {
          "countRows": false,
          "fields": "",
          "reducer": [
            "sum"
          ],
          "show": false
        },
        "showHeader": true
      },
      "pluginVersion": "12.1.0",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "${sourceSEM}"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH erosion_params AS (\n    SELECT instrument_id,\n           param_id,\n           (substring(param_name FROM '(\\d+)$')::int + 1) AS index\n    FROM parameters\n    WHERE instrument_id = $instrument\n      AND subsystem = 'AvaSem'\n      AND component ILIKE '%strip%'\n)\n\nSELECT\n    p.index AS \"Aperture index\",\n    d.value_num / 3600.0 AS \"Erosion time (h)\"\nFROM erosion_params p\nJOIN LATERAL (\n    SELECT value_num\n    FROM data d\n    WHERE d.instrument_id = p.instrument_id\n      AND d.param_id = p.param_id\n    ORDER BY d.time DESC\n    LIMIT 1\n) d ON true\nORDER BY p.index",
          "refId": "A",
          "sql": {
            "columns": [
              {
                "parameters": [],
                "type": "function"
              }
            ],
            "groupBy": [
              {
                "property": {
                  "type": "string"
                },
                "type": "groupBy"
              }
            ],
            "limit": 50
          }
        }
      ],
      "title": "SEM apertures",
      "type": "table"
    }
  ],
  "preload": false,
  "schemaVersion": 41,
  "tags": [
    "module",
    "sem"
  ],
  "templating": {
    "list": [
      {
        "allowCustomValue": false,
        "current": {
          "text": "sem",
          "value": "hm-sem"
        },
        "hide": 2,
        "name": "sourceSEM",
        "options": [],
        "query": "grafana-postgresql-datasource",
        "refresh": 1,
        "regex": "/^sem$/",
        "type": "datasource"
      },
      {
        "allowCustomValue": false,
        "current": {
          "text": "Arctis [Arctis] (9960963)",
          "value": "2"
        },
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "${sourceSEM}"
        },
        "definition": "SELECT\n  id AS __value,\n  name || ' [' || model || '] (' || serial || ')' AS __text\nFROM instruments\nORDER BY name;\n",
        "name": "instrument",
        "options": [],
        "query": "SELECT\n  id AS __value,\n  name || ' [' || model || '] (' || serial || ')' AS __text\nFROM instruments\nORDER BY name;\n",
        "refresh": 1,
        "regex": "",
        "sort": 1,
        "type": "query"
      }
    ]
  },
  "time": {
    "from": "now-7d",
    "to": "now"
  },
  "timepicker": {
    "refresh_intervals": []
  },
  "timezone": "Europe/London",
  "title": "Module | Apertures",
  "uid": "4395e67b-0345-47fd-55b9-684457567800",
  "version": 6
}